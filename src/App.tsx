import React from "react";

const App: React.FC = () => {
  return (
    <main style={{ padding: 24 }}>
      <h1>Noisy Pay</h1>
      <p>Welcome. Build is now clean with Lovable removed.</p>
    </main>
  );
};

export default App;
